import NoteBar from "@/screens/root/NoteBar";

const Root = () => {
    return (
        <div>
            <div className="flex justify-center">
                <NoteBar />
            </div>
        </div>
    );
};

export default Root;
